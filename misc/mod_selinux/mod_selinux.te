policy_module(mod_selinux, 1.2)

gen_require(`
	type httpd_t;
')

allow httpd_t self : process { setcurrent };

ifdef(`enable_mcs',`
	gen_require(`
		attribute mcssetcats;
	')
	typeattribute httpd_t mcssetcats;
')

ifdef(`enable_mls',`
	gen_require(`
		attribute mlsprocsetsl;
	')
	typeattribute httpd_t mlsprocsetsl;
')
